<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.doran.category.dao.CateforyDAO">

	<!-- category -->
	<resultMap id="category" type="categoryVO">
		<result property="categoryId" column="categoryId" />
		<result property="categoryName" column="categoryName" />
	</resultMap>
	
	<insert id="categoryInsert" parameterType="categoryVO">
		INSERT INTO categoryTbl(categoryName)
		VALUES(#{categoryName})
	</insert>
	
	<delete id="categoryDelete" parameterType="Integer">
		DELETE FROM categoryTbl 
		WHERE categoryId=#{categoryId} 
	</delete>
</mapper>